version: '3'

services:
  mysql:
    image: mysql:latest
    container_name: mysql_db
    restart: always
    environment:
      MYSQL_PASSWORD: ''
      MYSQL_ROOT_PASSWORD: 'password'
      MYSQL_DATABASE: ecommerce
    volumes:
      - ./data:/var/lib/mysql
    ports:
      - "3306:3306"